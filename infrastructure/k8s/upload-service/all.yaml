apiVersion: v1
kind: ConfigMap
metadata:
  name: upload-service-config
  namespace: finance-platform
data:
  NODE_ENV: production
  PORT: "3002"
---
apiVersion: v1
kind: Secret
metadata:
  name: upload-service-secret
  namespace: finance-platform
type: Opaque
stringData:
  DATABASE_URL: ""
  JWT_SECRET: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
  namespace: finance-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
        - name: upload-service
          image: REPLACE_WITH_ECR/upload-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: upload-service-config
            - secretRef:
                name: upload-service-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: finance-platform
spec:
  selector:
    app: upload-service
  ports:
    - port: 80
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: upload-service-hpa
  namespace: finance-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: upload-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
